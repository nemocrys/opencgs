general:
  name: cz_induction
  sim_dir: './simdata/_test'

geometry:
  dim: 2
  visualize: True

simulation:
  fast_simulation: False  # use relaxed convergence criteria given in solvers: fast-simulation section

special_geometry:  # TODO implement this!
  heating-test: False
  detached-crystal: False

diameter-iteration:
  r-min: 1.25e-3  # resulting from seed radius ( r_seed < r_crys)
  r-max: 1.e-2  # resulting from crucible radius
  dT-max: 1.e-2
  i-max: 10

transient:
  # TODO automatic steady-state simulation & restart from this base
